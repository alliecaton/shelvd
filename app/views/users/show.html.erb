<%= @user.email %> 

<div><%= link_to 'Create New Shelf', new_user_shelf_path(@user) if @user == current_user %></div>

<ul>
<%= @user.shelves.each do |shelf| %>
   <li><%= link_to shelf.name, shelf_path(shelf) %></li>
<% end %>
</ul>