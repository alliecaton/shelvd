<h2><%= @room.name %></h2>
<h4><%= @room.description %></h4>

<%= link_to "New Post",  new_reading_room_post_path(@room.id) %>

<div>
<h5>Active Users:</h5>

<% @room.users.each do |user| %>
    <%= user.email %>
<% end %>
</div>


<div>
<% @room.posts.each do |post| %>
<%= link_to post.user.email, user_path(post.user) %> - <%= post.content %> 
<% end %>
</div>